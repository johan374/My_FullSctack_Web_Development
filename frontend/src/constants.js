// Export a constant named ACCESS_TOKEN and assign it the value "acces".
// This constant can be used across the application to represent the key or identifier
// for an access token, typically used for authorization and making authenticated requests.
export const ACCESS_TOKEN = "access";

// Export a constant named REFRESH_TOKEN and assign it the value "refresh".
// This constant is used to represent the key or identifier for a refresh token,
// which is used to obtain a new access token when the current one expires.
export const REFRESH_TOKEN = "refresh";
 